const ravendesk_folder = '.ravendesk';

const config_file = `${ravendesk_folder}/config.js`;

const webpack_config_file = `${ravendesk_folder}/webpack.config.js`;

module.exports = {
	ravendesk_folder,
	config_file,
	webpack_config_file,
};
